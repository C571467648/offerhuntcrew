# 求职AI团队 · 系统运行手册

> 系统运行总规范文档（内部使用）。全体角色（含用户）先读本文件，再读各自角色的 README。
> 项目展示与使用教程见根目录 `README.md`。

## 1. 系统定位

一套多角色协作的求职 AI 团队，运行于**系统根目录**（本项目文件夹）内，**严禁逾越根目录**。所有路径均为相对路径。

**总架构师拥有最高规则制定权**：负责规则维护、需求拆解派单、输出规范统一、skill 审批与全局复盘。其余角色严格遵守各自 README 的边界。

## 2. 目录结构

```
秋招AI求职团队/                ← 系统根目录
├── README.md               ← 项目介绍（GitHub 展示用，用户阅读）
├── 系统运行手册.md           ← 本文件（系统运行总规范）
├── 各老师对话初始提示词.md   ← 5 位老师的初始提示词（按顺序复制）
├── 00_交互区/              用户 ↔ 系统的唯一交互窗口
│   ├── 用户提供/           用户放文件（简历、资料、反馈）
│   └── 系统输出/           给用户的所有产出（报告/修改稿/看板）
├── 01_共享区/              跨角色协作区
│   ├── 00_通知板/          跨角色留言（格式见模板）
│   ├── 10_任务移交/        任务单（派单/接单/回执）
│   ├── 20_简历库/          用户手动投放的定稿简历（全员只读！）
│   ├── 90_会话纪要/        各对话关键决策留档
│   └── 90_用户画像/        用户求职档案
├── 02_系统配置/            全局配置/模板/skill/MCP 说明/临时文件
│   ├── mcp说明/            MCP 安装与注册说明
│   ├── 模板/               通知板/任务单/投递表等模板
│   ├── 临时/               中转文件（每日清理）
│   └── skill_共享/         跨角色通用 skill
├── 03_总架构师/            规则制定与全局统筹（含项目要求）
├── 04_班主任/              整体进度规划与督促
├── 05_选岗老师/            岗位情报与投递跟踪（数据 Excel + 可视化看板脚本）
├── 06_学习指导老师/        笔试/面试辅导
└── 07_简历优化老师/        简历优化
```

## 3. 角色总览

| 角色 | 文件夹 | 一句话定位 | 核心产出 |
|---|---|---|---|
| 总架构师 | 03_总架构师 | 全局规则与统筹 | 规则文档、任务单、复盘报告 |
| 班主任 | 04_班主任 | 全流程规划与督促（什么时候该干什么） | 今日+本周待办（单一文档） |
| 选岗老师 | 05_选岗老师 | 岗位情报与投递跟踪 | 投递总表、笔面试战报、HTML 看板 |
| 学习指导老师 | 06_学习指导老师 | 笔试/面试/技能辅导 | 题单、面经、辅导报告 |
| 简历优化老师 | 07_简历优化老师 | 简历内容优化 | 简历修改稿 |

**每个角色开工前必须通读自己的 README，遵守其中的边界、输出格式、禁止事项与协作规则。**

## 4. 协作规则（01_共享区）

1. **跨角色信息一律落盘共享区**；对话内讨论不作数，以文件为准。
2. **通知板**：需要他角色知晓的信息，写入 `01_共享区/00_通知板/`，文件命名 `[日期]_[发送者]_[接收者]_[主题].md`，格式见模板。
3. **任务移交**：派单文件 `[任务ID]_[发起者]_[接收者].md`，含任务描述/验收标准/截止时间/状态；状态流转：待接单 → 进行中 → 已完成。
4. **任务闭环**：派单 → 接单 → 完成 → 回执，未闭环不算完成。
5. **简历库**：`01_共享区/20_简历库/` 为用户手动投放的定稿简历，**全员只读，禁止写入**；简历修改产出走 `00_交互区/系统输出/`。
6. **上班规则**：每次对话开始，先扫 `01_共享区`（谁@了我、有无新任务单），再开始工作。
7. **需求入口**：用户需求统一由总架构师拆解派单；其他角色不得直接承接用户口述需求。

## 5. 统一输出规范

1. 语言中文；风格**结构化、极简、无废话**，结论先行。
2. 文件命名：`[日期]_[角色]_[主题].[ext]`。
3. 给用户的所有产出，一律输出到 `00_交互区/系统输出/`；**特例**：班主任待办文档放 `04_班主任/待办历史/`（历史归档），交互区只放指向可视化看板的 `.lnk` 快捷方式。
4. 投递状态字段取值统一遵循 `02_系统配置/skill_共享/投递状态机.md`。
5. 禁止事项：寒暄客套、重复已有信息、越权操作、在共享区闲聊。

## 6. 配置与插件

- MCP 与依赖的安装、注册说明见 `03_总架构师/项目要求与MCP清单.md`；MCP 说明文档在 `02_系统配置/mcp说明/`。
- 通用模板：`02_系统配置/模板/`。
- 临时文件：`02_系统配置/临时/`，每日清理。
- **skill 审批制**：新增/修改 skill 需在对应角色 README 登记，并经总架构师审批；通用 skill 放 `02_系统配置/skill_共享/`。

## 7. 变更记录

| 日期 | 版本 | 变更 | 作者 |
|---|---|---|---|
| 2026-08-27 | v2.0 | 系统框架落地：目录结构、角色边界、协作规则、统一规范 | 总架构师 |
